local player = game:GetService("Players").LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local httpService = game:GetService("HttpService")
local teleportService = game:GetService("TeleportService")
local tweenService = game:GetService("TweenService")
local runService = game:GetService("RunService")
local uis = game:GetService("UserInputService")
local virtualUser = game:GetService("VirtualUser")

-- Safe GUI Parent
local function getSafeGuiParent()
    if gethui then return gethui() end
    local success, coreGui = pcall(function() return game:GetService("CoreGui") end)
    if success and coreGui then return coreGui end
    return player:WaitForChild("PlayerGui", 5) or player.PlayerGui
end

local parentGui = getSafeGuiParent()

-- Cleanup Old Instances
if parentGui:FindFirstChild("ApexEggHubShield") then
    parentGui.ApexEggHubShield:Destroy()
end
if workspace:FindFirstChild("ApexESP_Folder") then
    workspace.ApexESP_Folder:Destroy()
end

local espFolder = Instance.new("Folder")
espFolder.Name = "ApexESP_Folder"
espFolder.Parent = workspace

-- -------------------------------------------------------------
-- 1. Anti-Detection & Remote Hooking
-- -------------------------------------------------------------
local blockedRemotesCount = 0
local blockedRemoteNames = {
    ["Ban"] = true, ["Kick"] = true, ["GameWarning"] = true,
    ["Error"] = true, ["PlayerActivity"] = true, ["Teleporting"] = true,
    ["ServerMessage"] = true
}

pcall(function()
    if hookmetamethod then
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            if (method == "FireServer" or method == "InvokeServer") and self then
                if blockedRemoteNames[self.Name] then
                    blockedRemotesCount = blockedRemotesCount + 1
                    return nil
                end
            end
            return oldNamecall(self, ...)
        end)
    end
end)

-- -------------------------------------------------------------
-- 2. Anti-AFK Engine
-- -------------------------------------------------------------
local antiAfkEnabled = true
player.Idled:Connect(function()
    if antiAfkEnabled then
        virtualUser:CaptureController()
        virtualUser:ClickButton2(Vector2.new(0, 0))
    end
end)

-- Game Remotes & Services
local gameRemotes = rs:WaitForChild("Remotes", 4) and rs.Remotes:FindFirstChild("Game")
local plotRemotes = gameRemotes and gameRemotes:FindFirstChild("Plot")
local eggPickupRemote = gameRemotes and gameRemotes:FindFirstChild("EggPickup")
local tpPlotRemote = (plotRemotes and plotRemotes:FindFirstChild("TeleportToPlot")) or (gameRemotes and gameRemotes:FindFirstChild("TeleportToPlot"))
local eggPlacedRemote = gameRemotes and gameRemotes:FindFirstChild("EggPlaced")
local basketDropRemote = gameRemotes and gameRemotes:FindFirstChild("BasketDrop")
local eggCycleService = rs:FindFirstChild("GameServices") and rs.GameServices:FindFirstChild("EggCycle")

local eggList = {
    "Cherub Egg", "Blackhole Egg", "Galaxy Egg", "Aurora Egg", 
    "Soul Egg", "Sinister Egg", "Flaming Egg", "Dominus Egg", 
    "Asteroid Egg", "Skull Egg", "Crystal Egg", "Diamond Egg", 
    "Golden Egg", "Glass Egg", "Ice Egg", "Slime Egg", 
    "Flower Egg", "Mushroom Egg", "Leaf Egg", "Stone Egg", 
    "Easter Egg", "Cracked Egg", "Brown Egg", "White Egg"
}

-- อัพเดทตัวแปรระบบ
local currentTargetEgg = nil 
local selectedEggs = {} 
local maxSelection = 50
local eggButtons = {}
local originalBtnColors = {}

local isFarming = false
local isMinimized = false
local homeCFrame = nil
local activeHighlights = {}
local activeBillboards = {}
local lastRemoteCall = 0
local useFlashTP = false

-- -------------------------------------------------------------
-- Auto Server Hop Logic
-- -------------------------------------------------------------
local visitedServers = {}
local function hopLowPlayerServer()
    local placeId = game.PlaceId
    local currentJobId = game.JobId
    local reqUrl = string.format("https://games.roblox.com/v1/games/%s/servers/Public?sortOrder=Asc&limit=100", tostring(placeId))
    
    local success, response = pcall(function()
        if request then return request({Url = reqUrl, Method = "GET"})
        elseif http_request then return http_request({Url = reqUrl, Method = "GET"})
        elseif syn and syn.request then return syn.request({Url = reqUrl, Method = "GET"}) end
    end)
    
    if success and response and response.Body then
        local data = httpService:JSONDecode(response.Body)
        if data and data.data then
            local possibleServers = {}
            for _, srv in ipairs(data.data) do
                if srv.playing == 1 and srv.id ~= currentJobId and not visitedServers[srv.id] then
                    table.insert(possibleServers, srv.id)
                end
            end
            if #possibleServers > 0 then
                local targetServerId = possibleServers[math.random(1, #possibleServers)]
                visitedServers[targetServerId] = true
                teleportService:TeleportToPlaceInstance(placeId, targetServerId, player)
                return true
            end
        end
    end
    teleportService:Teleport(placeId, player)
end

-- -------------------------------------------------------------
-- FPS Boost (Potato Mode) Logic
-- -------------------------------------------------------------
local function enablePotatoMode()
    local lighting = game:GetService("Lighting")
    lighting.GlobalShadows = false
    lighting.FogEnd = 9e9
    lighting.Brightness = 1
    
    for _, v in pairs(lighting:GetChildren()) do
        if v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("BloomEffect") or v:IsA("Atmosphere") then
            v.Enabled = false
        end
    end

    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") then
            v.Material = Enum.Material.SmoothPlastic
            v.Reflectance = 0
            v.CastShadow = false
        elseif v:IsA("Decal") or v:IsA("Texture") then
            v:Destroy()
        elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then
            v.Enabled = false
        elseif v:IsA("Model") then
            local name = string.lower(v.Name)
            if name:find("tree") or name:find("bush") or name:find("grass") or name:find("plant") or name:find("leaf") then
                v:Destroy()
            end
        end
    end
    
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    end)
end

-- -------------------------------------------------------------
-- RESPONSIVE UI CONSTRUCTION
-- -------------------------------------------------------------
local sg = Instance.new("ScreenGui")
sg.Name = "ApexEggHubShield"
sg.ResetOnSpawn = false
sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
sg.Parent = parentGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 520, 0, 300)
mainFrame.Position = UDim2.new(0.5, -260, 0.5, -150)
mainFrame.BackgroundColor3 = Color3.fromRGB(13, 15, 20)
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Active = true
mainFrame.Parent = sg

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 12)
local mainStroke = Instance.new("UIStroke", mainFrame)
mainStroke.Color = Color3.fromRGB(38, 44, 62)
mainStroke.Thickness = 1.4

-- Dragging System
local dragging, dragInput, dragStart, startPos
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 36)
header.BackgroundColor3 = Color3.fromRGB(18, 21, 30)
header.BorderSizePixel = 0
header.Active = true
header.Parent = mainFrame

header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)

header.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
end)

uis.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -70, 1, 0)
titleLabel.Position = UDim2.new(0, 12, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "⚡ <font color=\"#60A5FA\"><b>APEX</b></font> INSPECTOR <font color=\"#10B981\">PRO</font>"
titleLabel.RichText = true
titleLabel.TextColor3 = Color3.fromRGB(240, 243, 248)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 13
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = header

local minBtn = Instance.new("TextButton")
minBtn.Size = UDim2.new(0, 24, 0, 24)
minBtn.Position = UDim2.new(1, -60, 0.5, -12)
minBtn.BackgroundColor3 = Color3.fromRGB(28, 34, 48)
minBtn.Text = "—"
minBtn.TextColor3 = Color3.fromRGB(180, 190, 210)
minBtn.Font = Enum.Font.GothamBold
minBtn.Parent = header
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 5)

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 24, 0, 24)
closeBtn.Position = UDim2.new(1, -30, 0.5, -12)
closeBtn.BackgroundColor3 = Color3.fromRGB(220, 38, 38)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = header
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 5)

local tabNav = Instance.new("Frame")
tabNav.Size = UDim2.new(1, -16, 0, 30)
tabNav.Position = UDim2.new(0, 8, 0, 42)
tabNav.BackgroundColor3 = Color3.fromRGB(19, 23, 33)
tabNav.BorderSizePixel = 0
tabNav.Parent = mainFrame
Instance.new("UICorner", tabNav).CornerRadius = UDim.new(0, 6)

local navLayout = Instance.new("UIListLayout", tabNav)
navLayout.FillDirection = Enum.FillDirection.Horizontal
navLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
navLayout.VerticalAlignment = Enum.VerticalAlignment.Center
navLayout.Padding = UDim.new(0, 6)

local tabs = {}
local tabButtons = {}

local function createTabButton(id, titleText)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.31, 0, 0, 24)
    btn.BackgroundColor3 = (id == "Farm") and Color3.fromRGB(59, 130, 246) or Color3.fromRGB(24, 29, 41)
    btn.Text = titleText
    btn.TextColor3 = (id == "Farm") and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(150, 160, 185)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10
    btn.Parent = tabNav
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    tabButtons[id] = btn
    return btn
end

createTabButton("Farm", "🎯 Sniper")
createTabButton("Security", "🛡️ Shield & Opt")
createTabButton("Weather", "⏱️ Cycle")

local pageContainer = Instance.new("Frame")
pageContainer.Size = UDim2.new(1, -16, 1, -80)
pageContainer.Position = UDim2.new(0, 8, 0, 76)
pageContainer.BackgroundTransparency = 1
pageContainer.Parent = mainFrame

-- TAB 1: SNIPER
local farmPage = Instance.new("Frame")
farmPage.Size = UDim2.new(1, 0, 1, 0)
farmPage.BackgroundTransparency = 1
farmPage.Parent = pageContainer
tabs["Farm"] = farmPage

local leftPanel = Instance.new("Frame")
leftPanel.Size = UDim2.new(0.44, 0, 1, -4)
leftPanel.BackgroundColor3 = Color3.fromRGB(16, 19, 28)
leftPanel.Parent = farmPage
Instance.new("UICorner", leftPanel).CornerRadius = UDim.new(0, 8)

local eggScroll = Instance.new("ScrollingFrame")
eggScroll.Size = UDim2.new(1, -6, 1, -6)
eggScroll.Position = UDim2.new(0, 3, 0, 3)
eggScroll.BackgroundTransparency = 1
eggScroll.BorderSizePixel = 0
eggScroll.ScrollBarThickness = 3
eggScroll.ScrollBarImageColor3 = Color3.fromRGB(59, 130, 246)
eggScroll.Parent = leftPanel
local eggListLayout = Instance.new("UIListLayout", eggScroll)
eggListLayout.Padding = UDim.new(0, 3)

eggListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    eggScroll.CanvasSize = UDim2.new(0, 0, 0, eggListLayout.AbsoluteContentSize.Y + 6)
end)

local rightPanel = Instance.new("Frame")
rightPanel.Size = UDim2.new(0.54, 0, 1, -4)
rightPanel.Position = UDim2.new(0.46, 0, 0, 0)
rightPanel.BackgroundTransparency = 1
rightPanel.Parent = farmPage

local statusCard = Instance.new("Frame")
statusCard.Size = UDim2.new(1, 0, 0, 42)
statusCard.BackgroundColor3 = Color3.fromRGB(21, 26, 37)
statusCard.Parent = rightPanel
Instance.new("UICorner", statusCard).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", statusCard).Color = Color3.fromRGB(36, 44, 63)

local targetLabel = Instance.new("TextLabel")
targetLabel.Size = UDim2.new(1, -12, 0, 16)
targetLabel.Position = UDim2.new(0, 6, 0, 3)
targetLabel.BackgroundTransparency = 1
targetLabel.Text = "🎯 TARGET: <font color=\"#F59E0B\">None Selected</font>"
targetLabel.RichText = true
targetLabel.TextColor3 = Color3.fromRGB(230, 235, 245)
targetLabel.Font = Enum.Font.GothamBold
targetLabel.TextSize = 10
targetLabel.TextXAlignment = Enum.TextXAlignment.Left
targetLabel.Parent = statusCard

local infoLabel = Instance.new("TextLabel")
infoLabel.Size = UDim2.new(1, -12, 0, 18)
infoLabel.Position = UDim2.new(0, 6, 0, 20)
infoLabel.BackgroundTransparency = 1
infoLabel.Text = "Status: Select an egg on the left..."
infoLabel.RichText = true
infoLabel.TextColor3 = Color3.fromRGB(140, 150, 175)
infoLabel.Font = Enum.Font.Gotham
infoLabel.TextSize = 9
infoLabel.TextXAlignment = Enum.TextXAlignment.Left
infoLabel.Parent = statusCard

local heavyCard = Instance.new("Frame")
heavyCard.Size = UDim2.new(1, 0, 0, 42)
heavyCard.Position = UDim2.new(0, 0, 0, 46)
heavyCard.BackgroundColor3 = Color3.fromRGB(21, 26, 37)
heavyCard.Parent = rightPanel
Instance.new("UICorner", heavyCard).CornerRadius = UDim.new(0, 6)
local heavyStroke = Instance.new("UIStroke", heavyCard)
heavyStroke.Color = Color3.fromRGB(234, 179, 8)
heavyStroke.Thickness = 1.2

local heavyTitle = Instance.new("TextLabel")
heavyTitle.Size = UDim2.new(1, -12, 0, 16)
heavyTitle.Position = UDim2.new(0, 6, 0, 3)
heavyTitle.BackgroundTransparency = 1
heavyTitle.Text = "👑 HEAVIEST: <font color=\"#EAB308\">Scanning...</font>"
heavyTitle.RichText = true
heavyTitle.TextColor3 = Color3.fromRGB(255, 220, 50)
heavyTitle.Font = Enum.Font.GothamBold
heavyTitle.TextSize = 10
heavyTitle.TextXAlignment = Enum.TextXAlignment.Left
heavyTitle.Parent = heavyCard

local heavyInfo = Instance.new("TextLabel")
heavyInfo.Size = UDim2.new(1, -12, 0, 22)
heavyInfo.Position = UDim2.new(0, 6, 0, 20)
heavyInfo.BackgroundTransparency = 1
heavyInfo.Text = "Weight: - | Dist: -"
heavyInfo.RichText = true
heavyInfo.TextColor3 = Color3.fromRGB(140, 150, 175)
heavyInfo.Font = Enum.Font.Gotham
heavyInfo.TextSize = 9
heavyInfo.TextXAlignment = Enum.TextXAlignment.Left
heavyInfo.Parent = heavyCard

local btnHolder = Instance.new("Frame")
btnHolder.Size = UDim2.new(1, 0, 1, -94)
btnHolder.Position = UDim2.new(0, 0, 0, 94)
btnHolder.BackgroundTransparency = 1
btnHolder.Parent = rightPanel

local btnListLayout = Instance.new("UIListLayout", btnHolder)
btnListLayout.FillDirection = Enum.FillDirection.Vertical
btnListLayout.Padding = UDim.new(0, 6)

local function createBtnRow()
    local r = Instance.new("Frame")
    r.Size = UDim2.new(1, 0, 0, 28)
    r.BackgroundTransparency = 1
    r.Parent = btnHolder
    return r
end

local function createActionBtn(text, color, parent, xPos, widthStr)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(widthStr, 0, 1, 0)
    btn.Position = UDim2.new(xPos, 0, 0, 0)
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10
    btn.AutoButtonColor = true
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    btn.Parent = parent
    return btn
end

local row1 = createBtnRow()
local tpBtn = createActionBtn("🚀 TP Target", Color3.fromRGB(59, 130, 246), row1, 0, 0.48)
local tpHvyBtn = createActionBtn("👑 TP Heaviest", Color3.fromRGB(217, 119, 6), row1, 0.52, 0.48)

local row2 = createBtnRow()
local homeBtn = createActionBtn("🏠 Set Base", Color3.fromRGB(30, 36, 52), row2, 0, 0.48)
local modeBtn = createActionBtn("🐢 Safe TP: ON", Color3.fromRGB(59, 130, 246), row2, 0.52, 0.48)

local row3 = createBtnRow()
row3.Size = UDim2.new(1, 0, 0, 32)
local farmBtn = createActionBtn("⚡ AUTO FARM", Color3.fromRGB(16, 185, 129), row3, 0, 1)

-- TAB 2 & 3 Setup (Security & Weather)
local secPage = Instance.new("Frame")
secPage.Size = UDim2.new(1, 0, 1, 0)
secPage.BackgroundTransparency = 1
secPage.Visible = false
secPage.Parent = pageContainer
tabs["Security"] = secPage

local secLeft = Instance.new("Frame")
secLeft.Size = UDim2.new(0.60, 0, 1, -4)
secLeft.BackgroundColor3 = Color3.fromRGB(18, 22, 31)
secLeft.Parent = secPage
Instance.new("UICorner", secLeft).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", secLeft).Color = Color3.fromRGB(36, 44, 63)

local secTitle = Instance.new("TextLabel")
secTitle.Size = UDim2.new(1, -16, 0, 20)
secTitle.Position = UDim2.new(0, 8, 0, 6)
secTitle.BackgroundTransparency = 1
secTitle.Text = "🛡️ SECURITY LOGS"
secTitle.TextColor3 = Color3.fromRGB(16, 185, 129)
secTitle.Font = Enum.Font.GothamBold
secTitle.TextSize = 11
secTitle.TextXAlignment = Enum.TextXAlignment.Left
secTitle.Parent = secLeft

local secLogs = Instance.new("TextLabel")
secLogs.Size = UDim2.new(1, -16, 1, -30)
secLogs.Position = UDim2.new(0, 8, 0, 26)
secLogs.BackgroundTransparency = 1
secLogs.Text = string.format("• Anti-AFK (20m Timeout): ACTIVE\n• Ban/Kick Remotes: DROPPED\n• Total Intercepted Traps: %d", blockedRemotesCount)
secLogs.TextColor3 = Color3.fromRGB(190, 200, 225)
secLogs.Font = Enum.Font.Gotham
secLogs.TextSize = 10
secLogs.TextXAlignment = Enum.TextXAlignment.Left
secLogs.TextYAlignment = Enum.TextYAlignment.Top
secLogs.Parent = secLeft

local secRight = Instance.new("Frame")
secRight.Size = UDim2.new(0.38, 0, 1, -4)
secRight.Position = UDim2.new(0.62, 0, 0, 0)
secRight.BackgroundColor3 = Color3.fromRGB(21, 26, 37)
secRight.Parent = secPage
Instance.new("UICorner", secRight).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", secRight).Color = Color3.fromRGB(36, 44, 63)

local hopTitle = Instance.new("TextLabel")
hopTitle.Size = UDim2.new(1, -8, 0, 18)
hopTitle.Position = UDim2.new(0, 4, 0, 6)
hopTitle.BackgroundTransparency = 1
hopTitle.Text = "🌐 CONTROL"
hopTitle.TextColor3 = Color3.fromRGB(96, 165, 250)
hopTitle.Font = Enum.Font.GothamBold
hopTitle.TextSize = 10
hopTitle.Parent = secRight

local hopBtn = Instance.new("TextButton")
hopBtn.Size = UDim2.new(1, -16, 0, 32)
hopBtn.Position = UDim2.new(0, 8, 0, 26)
hopBtn.BackgroundColor3 = Color3.fromRGB(59, 130, 246)
hopBtn.Text = "🚀 Hop Server\n(1 Player)"
hopBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
hopBtn.Font = Enum.Font.GothamBold
hopBtn.TextSize = 9
hopBtn.AutoButtonColor = true
hopBtn.Parent = secRight
Instance.new("UICorner", hopBtn).CornerRadius = UDim.new(0, 6)

hopBtn.Activated:Connect(function()
    hopBtn.Text = "🔄 Finding..."
    hopLowPlayerServer()
end)

local fpsBtn = Instance.new("TextButton")
fpsBtn.Size = UDim2.new(1, -16, 0, 32)
fpsBtn.Position = UDim2.new(0, 8, 0, 64)
fpsBtn.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
fpsBtn.Text = "🥔 Boost FPS\n(Clear Trees/Gfx)"
fpsBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
fpsBtn.Font = Enum.Font.GothamBold
fpsBtn.TextSize = 9
fpsBtn.AutoButtonColor = true
fpsBtn.Parent = secRight
Instance.new("UICorner", fpsBtn).CornerRadius = UDim.new(0, 6)

fpsBtn.Activated:Connect(function()
    fpsBtn.Text = "✅ Boosted!"
    fpsBtn.BackgroundColor3 = Color3.fromRGB(16, 185, 129)
    enablePotatoMode()
end)

local weatherPage = Instance.new("Frame")
weatherPage.Size = UDim2.new(1, 0, 1, 0)
weatherPage.BackgroundTransparency = 1
weatherPage.Visible = false
weatherPage.Parent = pageContainer
tabs["Weather"] = weatherPage

local wLeft = Instance.new("Frame")
wLeft.Size = UDim2.new(0.48, 0, 1, -4)
wLeft.BackgroundColor3 = Color3.fromRGB(21, 26, 37)
wLeft.Parent = weatherPage
Instance.new("UICorner", wLeft).CornerRadius = UDim.new(0, 8)

local cycleTitle = Instance.new("TextLabel")
cycleTitle.Size = UDim2.new(1, 0, 0, 20)
cycleTitle.Position = UDim2.new(0, 0, 0, 12)
cycleTitle.BackgroundTransparency = 1
cycleTitle.Text = "🔄 CYCLE TIMER"
cycleTitle.TextColor3 = Color3.fromRGB(96, 165, 250)
cycleTitle.Font = Enum.Font.GothamBold
cycleTitle.TextSize = 12
cycleTitle.Parent = wLeft

local countdownLabel = Instance.new("TextLabel")
countdownLabel.Size = UDim2.new(1, 0, 0, 45)
countdownLabel.Position = UDim2.new(0, 0, 0, 38)
countdownLabel.BackgroundTransparency = 1
countdownLabel.Text = "07:59"
countdownLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
countdownLabel.Font = Enum.Font.GothamBold
countdownLabel.TextSize = 32
countdownLabel.Parent = wLeft

local wRight = Instance.new("Frame")
wRight.Size = UDim2.new(0.50, 0, 1, -4)
wRight.Position = UDim2.new(0.50, 0, 0, 0)
wRight.BackgroundColor3 = Color3.fromRGB(18, 22, 31)
wRight.Parent = weatherPage
Instance.new("UICorner", wRight).CornerRadius = UDim.new(0, 8)

local weatherHeader = Instance.new("TextLabel")
weatherHeader.Size = UDim2.new(1, -16, 0, 22)
weatherHeader.Position = UDim2.new(0, 10, 0, 8)
weatherHeader.BackgroundTransparency = 1
weatherHeader.Text = "🌤️ WEATHER"
weatherHeader.TextColor3 = Color3.fromRGB(245, 158, 11)
weatherHeader.Font = Enum.Font.GothamBold
weatherHeader.TextSize = 11
weatherHeader.TextXAlignment = Enum.TextXAlignment.Left
weatherHeader.Parent = wRight

local function switchTab(tabId)
    for id, page in pairs(tabs) do page.Visible = (id == tabId) end
    for id, btn in pairs(tabButtons) do
        if id == tabId then
            btn.BackgroundColor3 = Color3.fromRGB(59, 130, 246)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            btn.BackgroundColor3 = Color3.fromRGB(24, 29, 41)
            btn.TextColor3 = Color3.fromRGB(150, 160, 185)
        end
    end
end
for id, btn in pairs(tabButtons) do
    btn.Activated:Connect(function() switchTab(id) end)
end

-- Resize Logic
local resizeHandle = Instance.new("Frame")
resizeHandle.Size = UDim2.new(0, 18, 0, 18)
resizeHandle.Position = UDim2.new(1, -18, 1, -18)
resizeHandle.BackgroundColor3 = Color3.fromRGB(59, 130, 246)
resizeHandle.BackgroundTransparency = 0.5
resizeHandle.BorderSizePixel = 0
resizeHandle.ZIndex = 10
resizeHandle.Parent = mainFrame
Instance.new("UICorner", resizeHandle).CornerRadius = UDim.new(0, 6)

local resizeIcon = Instance.new("TextLabel")
resizeIcon.Size = UDim2.new(1, 0, 1, 0)
resizeIcon.BackgroundTransparency = 1
resizeIcon.Text = "◢"
resizeIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
resizeIcon.Font = Enum.Font.GothamBold
resizeIcon.TextSize = 12
resizeIcon.ZIndex = 11
resizeIcon.Parent = resizeHandle

local resizing = false
local resizeStartPos, resizeStartSize

resizeHandle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        resizing = true
        resizeStartPos = input.Position
        resizeStartSize = mainFrame.AbsoluteSize
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then resizing = false end
        end)
    end
end)

uis.InputChanged:Connect(function(input)
    if resizing and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - resizeStartPos
        local newWidth = math.clamp(resizeStartSize.X + delta.X, 420, 800)
        local newHeight = math.clamp(resizeStartSize.Y + delta.Y, 240, 450)
        mainFrame.Size = UDim2.new(0, newWidth, 0, newHeight)
        
        local btnHeight = math.clamp((newHeight - 210) / 3, 24, 40)
        row1.Size = UDim2.new(1, 0, 0, btnHeight)
        row2.Size = UDim2.new(1, 0, 0, btnHeight)
        row3.Size = UDim2.new(1, 0, 0, btnHeight + 4)
    end
end)

-- -------------------------------------------------------------
-- Inspector & Parse
-- -------------------------------------------------------------
local function findAutoPlotCFrame()
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local pn = player.Name
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("TextLabel") and obj.Text:find(pn) then
            local p = obj:FindFirstAncestorWhichIsA("Model")
            local s = p and (p:FindFirstChild("Spawn") or p:FindFirstChild("SpawnLocation"))
            if s then return s.CFrame + Vector3.new(0, 3, 0) end
        end
    end
    return hrp and hrp.CFrame or nil
end

local function getRarityInfo(eggName)
    local idx = 99
    for i, name in ipairs(eggList) do
        if string.lower(name) == string.lower(eggName) then idx = i break end
    end
    if idx <= 4 then return Color3.fromRGB(192, 132, 252), "Ethereal/Godly"
    elseif idx <= 10 then return Color3.fromRGB(251, 146, 60), "Mythic"
    elseif idx <= 16 then return Color3.fromRGB(56, 189, 248), "Legendary"
    else return Color3.fromRGB(156, 163, 175), "Common" end
end

local function isWildEgg(item)
    local curr = item.Parent
    while curr and curr ~= workspace do
        local n = string.lower(curr.Name)
        if n:find("plot") or n:find("nest") or n:find("base") or curr:FindFirstChild("Humanoid") then return false end
        curr = curr.Parent
    end
    return true
end

local function parseEggWeight(egg)
    local maxFoundWeight = 0
    local attrs = egg:GetAttributes()
    for k, v in pairs(attrs) do
        local lk = string.lower(k)
        if (lk == "weight" or lk == "multiplier" or lk == "mass" or lk == "kg") and type(v) == "number" then return v end
    end

    for _, v in pairs(egg:GetDescendants()) do
        if v:IsA("NumberValue") or v:IsA("IntValue") then
            local n = string.lower(v.Name)
            if n == "weight" or n == "multiplier" or n == "mass" or n == "kg" then
                if v.Value > maxFoundWeight then maxFoundWeight = v.Value end
            end
        elseif v:IsA("TextLabel") and v.Visible then
            local txt = string.lower(v.Text)
            local numStr = string.match(txt, "([%d%.]+)%s*kg")
            if not numStr then
                local lbsStr = string.match(txt, "([%d%.]+)%s*lbs")
                if lbsStr then
                    local lbsNum = tonumber(lbsStr)
                    if lbsNum then numStr = tostring(lbsNum * 0.453592) end
                end
            end
            if numStr then
                local num = tonumber(numStr)
                if num and num > maxFoundWeight then maxFoundWeight = num end
            end
        end
    end
    if maxFoundWeight > 0 then return maxFoundWeight end

    local part = egg:FindFirstChild("Handle") or egg:FindFirstChild("EggBase") or egg.PrimaryPart or egg:FindFirstChildWhichIsA("BasePart")
    if part then
        local vol = part.Size.X * part.Size.Y * part.Size.Z
        return math.floor(vol * 5) / 10
    end
    return 0
end

local function getFormattedWeight(egg)
    local w = parseEggWeight(egg)
    if w > 0 then return string.format("%.1f kg", w) else return "?? kg (Hidden)" end
end

local function clearESP()
    espFolder:ClearAllChildren()
    for _, hl in pairs(activeHighlights) do if hl then hl:Destroy() end end
    activeHighlights = {}
    activeBillboards = {}
end

local function createDetailedESP(egg, part, isHeaviest)
    local themeColor, tierName = getRarityInfo(egg.Name)
    if isHeaviest then themeColor = Color3.fromRGB(234, 179, 8) end
    
    local hl = Instance.new("Highlight")
    hl.Adornee = egg
    hl.FillColor = themeColor
    hl.OutlineColor = isHeaviest and Color3.fromRGB(255, 230, 0) or Color3.fromRGB(255, 255, 255)
    hl.FillTransparency = 0.4
    hl.OutlineTransparency = 0.1
    hl.Parent = espFolder
    table.insert(activeHighlights, hl)
    
    local bg = Instance.new("BillboardGui")
    bg.Adornee = part
    bg.Size = UDim2.new(0, 160, 0, 56)
    bg.AlwaysOnTop = true
    bg.StudsOffset = Vector3.new(0, 2.8, 0)
    bg.Parent = espFolder
    
    local card = Instance.new("Frame")
    card.Size = UDim2.new(1, 0, 1, 0)
    card.BackgroundColor3 = Color3.fromRGB(12, 14, 20)
    card.BackgroundTransparency = 0.12
    card.BorderSizePixel = 0
    card.Parent = bg
    Instance.new("UICorner", card).CornerRadius = UDim.new(0, 8)
    local stroke = Instance.new("UIStroke", card)
    stroke.Color = themeColor
    stroke.Thickness = isHeaviest and 1.8 or 1.2
    
    local titleText = (isHeaviest and "👑 [HEAVIEST] " or "✨ ") .. egg.Name
    local nameLbl = Instance.new("TextLabel")
    nameLbl.Size = UDim2.new(1, -8, 0, 18)
    nameLbl.Position = UDim2.new(0, 4, 0, 2)
    nameLbl.BackgroundTransparency = 1
    nameLbl.TextColor3 = isHeaviest and Color3.fromRGB(255, 220, 50) or Color3.fromRGB(255, 255, 255)
    nameLbl.Text = titleText
    nameLbl.Font = Enum.Font.GothamBold
    nameLbl.TextSize = 10
    nameLbl.TextTruncate = Enum.TextTruncate.AtEnd
    nameLbl.Parent = card
    
    local tierLbl = Instance.new("TextLabel")
    tierLbl.Size = UDim2.new(1, -8, 0, 12)
    tierLbl.Position = UDim2.new(0, 4, 0, 18)
    tierLbl.BackgroundTransparency = 1
    tierLbl.TextColor3 = themeColor
    tierLbl.Text = "Tier: " .. tierName
    tierLbl.Font = Enum.Font.GothamBold
    tierLbl.TextSize = 8
    tierLbl.TextXAlignment = Enum.TextXAlignment.Left
    tierLbl.Parent = card
    
    local tagHolder = Instance.new("Frame")
    tagHolder.Size = UDim2.new(1, -8, 0, 18)
    tagHolder.Position = UDim2.new(0, 4, 0, 32)
    tagHolder.BackgroundTransparency = 1
    tagHolder.Parent = card
    local hLayout = Instance.new("UIListLayout", tagHolder)
    hLayout.FillDirection = Enum.FillDirection.Horizontal
    hLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    hLayout.Padding = UDim.new(0, 4)
    
    local wTag = Instance.new("TextLabel")
    wTag.Size = UDim2.new(0.48, 0, 1, 0)
    wTag.BackgroundColor3 = Color3.fromRGB(24, 30, 44)
    wTag.TextColor3 = Color3.fromRGB(255, 215, 0)
    wTag.Font = Enum.Font.GothamBold
    wTag.TextSize = 8
    wTag.Text = "⚖️ " .. getFormattedWeight(egg)
    wTag.Parent = tagHolder
    Instance.new("UICorner", wTag).CornerRadius = UDim.new(0, 4)
    
    local dTag = Instance.new("TextLabel")
    dTag.Size = UDim2.new(0.48, 0, 1, 0)
    dTag.BackgroundColor3 = Color3.fromRGB(20, 26, 38)
    dTag.TextColor3 = Color3.fromRGB(0, 229, 255)
    dTag.Font = Enum.Font.GothamBold
    dTag.TextSize = 8
    dTag.Text = "📍 ..."
    dTag.Parent = tagHolder
    Instance.new("UICorner", dTag).CornerRadius = UDim.new(0, 4)
    
    table.insert(activeBillboards, {Label = dTag, Part = part})
end

-- ==========================================
-- 1. ฟังก์ชันค้นหาสำหรับโชว์ ESP / ข้อมูล (อิงตาม currentTargetEgg)
-- ==========================================
local function scanAndInspect()
    local selectedMatches = {}
    local heaviestEgg, maxW = nil, -1
    
    for _, item in pairs(workspace:GetDescendants()) do
        if (item:IsA("Model") or item:IsA("Tool")) and isWildEgg(item) then
            local isKnown = false
            for _, en in ipairs(eggList) do
                if string.find(string.lower(item.Name), string.lower(en)) then isKnown = true break end
            end
            
            if isKnown then
                local w = parseEggWeight(item)
                if w > maxW then
                    maxW = w
                    heaviestEgg = item
                elseif w == 0 and maxW == -1 then
                    heaviestEgg = item
                end
                
                if currentTargetEgg and string.find(string.lower(item.Name), string.lower(currentTargetEgg)) then
                    table.insert(selectedMatches, item)
                end
            end
        end
    end
    return selectedMatches, heaviestEgg
end

-- ==========================================
-- 2. ฟังก์ชันค้นหาเฉพาะระบบ Auto Farm (อิงตาม selectedEggs Queue)
-- ==========================================
local function scanForFarmQueue()
    local eggsByName = {}
    for _, item in pairs(workspace:GetDescendants()) do
        if (item:IsA("Model") or item:IsA("Tool")) and isWildEgg(item) then
            for _, en in ipairs(eggList) do
                if string.find(string.lower(item.Name), string.lower(en)) then 
                    if not eggsByName[en] then eggsByName[en] = {} end
                    table.insert(eggsByName[en], item)
                    break 
                end
            end
        end
    end
    
    -- เรียงตามคิวใบที่ 1 ถึง 50 หาเจอใบไหนก่อนให้ส่งกลับไปฟาร์ม
    for index, queueName in ipairs(selectedEggs) do
        if eggsByName[queueName] and #eggsByName[queueName] > 0 then
            return eggsByName[queueName][1], index
        end
    end
    return nil, nil
end

local function updateInspectorESP()
    clearESP()
    local myChar = player.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    
    local selectedMatches, heaviestEgg = scanAndInspect()
    
    if heaviestEgg then
        local p = heaviestEgg:FindFirstChild("Handle") or heaviestEgg:FindFirstChild("EggBase") or heaviestEgg.PrimaryPart or heaviestEgg:FindFirstChildWhichIsA("BasePart")
        local dist = (myHRP and p) and math.floor((myHRP.Position - p.Position).Magnitude) or 0
        heavyTitle.Text = string.format("👑 HEAVIEST: <font color=\"#EAB308\"><b>%s</b></font>", heaviestEgg.Name)
        heavyInfo.Text = string.format("W: <font color=\"#F59E0B\"><b>%s</b></font> | Dist: <b>%dm</b>", getFormattedWeight(heaviestEgg), dist)
        if p then createDetailedESP(heaviestEgg, p, true) end
    else
        heavyTitle.Text = "👑 HEAVIEST: <font color=\"#EF4444\">Not Found</font>"
        heavyInfo.Text = "Weight: - | Dist: -"
    end
    
    if currentTargetEgg then
        if #selectedMatches > 0 then
            local nearestDist = math.huge
            local bestMatch = selectedMatches[1]
            for _, egg in pairs(selectedMatches) do
                local p = egg:FindFirstChild("Handle") or egg:FindFirstChild("EggBase") or egg.PrimaryPart or egg:FindFirstChildWhichIsA("BasePart")
                if p then
                    local d = myHRP and math.floor((myHRP.Position - p.Position).Magnitude) or 0
                    if d < nearestDist then nearestDist = d bestMatch = egg end
                    if egg ~= heaviestEgg then createDetailedESP(egg, p, false) end
                end
            end
            infoLabel.Text = string.format("Found: <font color=\"#10B981\"><b>%d</b></font> | Nearest: <b>%dm</b> | W: <b>%s</b>", #selectedMatches, nearestDist, getFormattedWeight(bestMatch))
        else
            infoLabel.Text = "Status: <font color=\"#EF4444\">Target Not Spawned</font>"
        end
    end
end

-- ==========================================
-- ปุ่มกด (แยก Click เดี่ยว และ กดค้าง 3 วิ)
-- ==========================================
for i, eggName in ipairs(eggList) do
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(1, -4, 0, 24)
    
    local origColor
    if i <= 4 then origColor = Color3.fromRGB(110, 30, 150)
    elseif i <= 10 then origColor = Color3.fromRGB(160, 60, 10)
    else origColor = Color3.fromRGB(26, 32, 45) end
    
    b.BackgroundColor3 = origColor
    b.Text = "  " .. eggName
    b.TextColor3 = Color3.fromRGB(240, 245, 255)
    b.Font = Enum.Font.GothamSemibold
    b.TextSize = 10
    b.TextXAlignment = Enum.TextXAlignment.Left
    b.AutoButtonColor = true
    b.Parent = eggScroll
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 4)
    
    eggButtons[eggName] = b
    originalBtnColors[eggName] = origColor
    
    local pressStartPos = nil
    local isPressing = false

    -- [คลิกธรรมดา] ตั้งเป้าหมาย Target ดูค่าพลัง/วาร์ปไปหา
    b.Activated:Connect(function()
        currentTargetEgg = eggName
        targetLabel.Text = "🎯 TARGET: <font color=\"#60A5FA\"><b>" .. currentTargetEgg .. "</b></font>"
        updateInspectorESP()
    end)

    -- [กดค้าง] เริ่มจับเวลา 3 วิ
    b.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isPressing = true
            pressStartPos = input.Position

            task.delay(3, function()
                if isPressing then
                    isPressing = false 
                    
                    local idx = table.find(selectedEggs, eggName)
                    if idx then
                        table.remove(selectedEggs, idx)
                    else
                        if #selectedEggs < maxSelection then
                            table.insert(selectedEggs, eggName)
                        end
                    end
                    
                    for eName, btn in pairs(eggButtons) do
                        local currentIdx = table.find(selectedEggs, eName)
                        if currentIdx then
                            btn.Text = " [" .. currentIdx .. "] " .. eName
                            btn.BackgroundColor3 = Color3.fromRGB(16, 185, 129) 
                        else
                            btn.Text = "  " .. eName
                            btn.BackgroundColor3 = originalBtnColors[eName]
                        end
                    end
                    
                    if #selectedEggs == 0 and isFarming then
                        isFarming = false
                        farmBtn.Text = "⚡ AUTO FARM"
                        farmBtn.BackgroundColor3 = Color3.fromRGB(16, 185, 129)
                    end
                end
            end)
        end
    end)

    b.InputChanged:Connect(function(input)
        if isPressing and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            if pressStartPos and (input.Position - pressStartPos).Magnitude > 15 then
                isPressing = false 
            end
        end
    end)

    b.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isPressing = false 
        end
    end)
end

-- ===========================================================
-- Dual Grab Mode Logic (Toggle)
-- ===========================================================
modeBtn.Activated:Connect(function()
    useFlashTP = not useFlashTP
    if useFlashTP then
        modeBtn.Text = "⚡ Flash TP: ON"
        modeBtn.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
    else
        modeBtn.Text = "🐢 Safe TP: ON"
        modeBtn.BackgroundColor3 = Color3.fromRGB(59, 130, 246)
    end
end)

local function grabAndReturn(egg)
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp or not egg or not egg.Parent then return end
    
    local targetPart = egg:FindFirstChild("Handle") or egg:FindFirstChild("EggBase") or egg.PrimaryPart or egg:FindFirstChildWhichIsA("BasePart")
    if not targetPart then return end
    if not homeCFrame then homeCFrame = findAutoPlotCFrame() end
    
    hrp.AssemblyLinearVelocity = Vector3.zero
    
    if useFlashTP then
        local originalPos = homeCFrame or hrp.CFrame
        hrp.CFrame = targetPart.CFrame + Vector3.new(0, 1.5, 0)
        
        if os.clock() - lastRemoteCall > 0.1 then
            lastRemoteCall = os.clock()
            pcall(function()
                if eggPickupRemote then eggPickupRemote:FireServer(egg) end
                local prompt = egg:FindFirstChildWhichIsA("ProximityPrompt", true)
                if prompt then prompt.HoldDuration = 0 if fireproximityprompt then fireproximityprompt(prompt) end end
            end)
        end
        task.wait(0.05)
        
        hrp.CFrame = originalPos
        pcall(function()
            if tpPlotRemote then tpPlotRemote:FireServer() end
            if basketDropRemote then basketDropRemote:FireServer() end
            if eggPlacedRemote then eggPlacedRemote:FireServer() end
        end)
    else
        hrp.CFrame = targetPart.CFrame + Vector3.new(0, 1.5, 0)
        task.wait(0.2)
        
        if os.clock() - lastRemoteCall > 0.25 then
            lastRemoteCall = os.clock()
            pcall(function()
                if eggPickupRemote then eggPickupRemote:FireServer(egg) end
                local prompt = egg:FindFirstChildWhichIsA("ProximityPrompt", true)
                if prompt then prompt.HoldDuration = 0 if fireproximityprompt then fireproximityprompt(prompt) end end
            end)
        end
        task.wait(0.25)
        
        if homeCFrame then hrp.CFrame = homeCFrame end
        pcall(function()
            if tpPlotRemote then tpPlotRemote:FireServer() end
            if basketDropRemote then basketDropRemote:FireServer() end
            if eggPlacedRemote then eggPlacedRemote:FireServer() end
        end)
    end
end

-- Button Activations
tpBtn.Activated:Connect(function()
    local selectedMatches, _ = scanAndInspect()
    if #selectedMatches > 0 then grabAndReturn(selectedMatches[1]) end
end)

tpHvyBtn.Activated:Connect(function()
    local _, heaviestEgg = scanAndInspect()
    if heaviestEgg then grabAndReturn(heaviestEgg) end
end)

homeBtn.Activated:Connect(function()
    local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        homeCFrame = hrp.CFrame
        homeBtn.Text = "✅ Saved!"
        task.delay(1.5, function() homeBtn.Text = "🏠 Set Base" end)
    end
end)

farmBtn.Activated:Connect(function()
    if #selectedEggs == 0 then return end
    isFarming = not isFarming
    if isFarming then
        homeCFrame = findAutoPlotCFrame()
        farmBtn.Text = "⏹ STOP FARM"
        farmBtn.BackgroundColor3 = Color3.fromRGB(220, 38, 38)
    else
        farmBtn.Text = "⚡ AUTO FARM"
        farmBtn.BackgroundColor3 = Color3.fromRGB(16, 185, 129)
    end
end)

minBtn.Activated:Connect(function()
    isMinimized = not isMinimized
    tweenService:Create(mainFrame, TweenInfo.new(0.2), {Size = UDim2.new(0, mainFrame.AbsoluteSize.X, 0, isMinimized and 36 or 300)}):Play()
    minBtn.Text = isMinimized and "+" or "—"
end)

closeBtn.Activated:Connect(function()
    isFarming = false
    clearESP()
    sg:Destroy()
end)

-- Real-Time Loops
runService.RenderStepped:Connect(function()
    local myChar = player.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if myHRP and #activeBillboards > 0 then
        for _, item in pairs(activeBillboards) do
            if item.Part and item.Part.Parent and item.Label and item.Label.Parent then
                local dist = math.floor((myHRP.Position - item.Part.Position).Magnitude)
                item.Label.Text = string.format("📍 %dm", dist)
            end
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        local timeLeft = 0
        pcall(function()
            if eggCycleService then
                local cycleMod = require(eggCycleService)
                if cycleMod.SecondsRemaining then timeLeft = math.floor(cycleMod.SecondsRemaining()) end
            else timeLeft = 480 - (math.floor(os.time()) % 480) end
        end)
        countdownLabel.Text = string.format("%02d:%02d", math.floor(timeLeft / 60), timeLeft % 60)
        secLogs.Text = string.format("• Anti-AFK (20m Timeout): ACTIVE\n• Ban/Kick Remotes: DROPPED\n• Total Intercepted Traps: %d", blockedRemotesCount)
    end
end)

task.spawn(function()
    while task.wait(1.5) do
        if not isFarming then updateInspectorESP() end
    end
end)

local lastQueueIndex = nil
local hasUnequippedOnEmpty = false -- ตัวจับสถานะสำหรับบังคับมือเปล่า 1 ครั้งตอนไข่หมด

task.spawn(function()
    while task.wait(0.4) do
        if isFarming and #selectedEggs > 0 then
            local targetToFarm, currentQueueIndex = scanForFarmQueue()
            if targetToFarm then
                
                hasUnequippedOnEmpty = false -- รีเซ็ตสถานะเมื่อเจอไข่ในแมพ
                
                if lastQueueIndex and currentQueueIndex > lastQueueIndex then
                    
                    task.wait(1) -- รอ 1 วิ
                    
                    local char = player.Character
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    if hum then
                        pcall(function() hum:UnequipTools() end)
                    end
                    
                    task.wait(2) -- รออีก 2 วิ
                    
                    lastQueueIndex = currentQueueIndex
                else
                    grabAndReturn(targetToFarm)
                    lastQueueIndex = currentQueueIndex
                    task.wait(useFlashTP and 0.2 or 0.4)
                end
                
            else
                -- [เพิ่มใหม่] เงื่อนไขเมื่อไข่ในลิสต์ไม่มีเหลือในแมพแล้ว
                lastQueueIndex = nil
                
                if not hasUnequippedOnEmpty then
                    local char = player.Character
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    if hum then
                        pcall(function() hum:UnequipTools() end)
                    end
                    hasUnequippedOnEmpty = true -- สั่งทำแค่ 1 ครั้งแล้วหยุด
                end
            end
        else
            lastQueueIndex = nil
            hasUnequippedOnEmpty = false
        end
    end
end)

